<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="../css/reset.css" />
    <link rel="stylesheet" href="../css/global.css" />
    <link rel="stylesheet" href="../css/carrinho.css" />
    <title>Carrinho</title>
  </head>
  <body>
    <%- include('./components/header.ejs') %>

    <main class="container">
      <div class="cart">
        <div class="product">
          <div class="product-info">
            <section>
              <img
                src="<%=produto.image%>"
                alt="Placa de vídeo" />
              <p class="product-name">
                <%=produto.name%>
              </p>
            </section>
            <section class="bottom-section">
              <p>
                Quantidade
              </p>
                <button ><span class="material-symbols-outlined" data-button="+"> add </span></button>
                <input data-quantity value="0" disabled></input>
                <button ><span class="material-symbols-outlined" data-button="-"> remove </span></button>
              
              <span><%=new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(produto.price)%></span>
            </section>
          </div>
          <div class="frete">
            <form class="cep">
              <label for="cep-number">Digite aqui seu CEP</label>
              <input type="text" id="cep-number" name="cep-number" />
            </form>
            <button>Calcular</button>
          </div>
        </div>
        <div class="price-info">
          <li class="info">
            <p>Subtotal (1 item )</p>
            <p>R$ <%=produto.price%></p>
          </li>
          <li class="info">
            <p>Frete DigitalHouse</p>
            <p>Gratis</p>
          </li>
          <li class="info">
            <p>Cupom de Desconto</p>
            <p>Adicionar</p>
          </li>
          <li class="info">
            <p>Descontos</p>
            <p>R$ 0,00</p>
          </li>
          <li class="info">
            <p>Valor Total</p>
            <p>R$ <%=produto.price%></p>
          </li>
          <a href="/listagem-produtos"><button class="add-more">Escolher mais produtos</button></a>
          <button class="finish">FINALIZAR</button>
        </div>
      </div>
      <%- include('./components/footer.ejs') %>
      <script src="../js/carrinho.js"></script>
    </main>
  </body>
</html>
